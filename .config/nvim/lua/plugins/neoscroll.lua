return {
  "karb94/neoscroll.nvim",
  opts = {
    -- Set default easing function ("quadratic", "cubic", "sine", etc.)
    easing = "quadratic",
    hide_cursor = true,   -- Hide cursor while scrolling for a cleaner look
    stop_eof = true,      -- Stop scrolling when reaching the end of the file
    respect_scrolloff = false,
  },
}
